clock skew：是指时钟到达源寄存器和目的寄存器的时间差。
clock latency：从时钟源到任意接收端（如寄存器）的总延迟时间，包括源延迟和导线延迟。
- 源延迟：指到达设计之前，时钟消耗的延迟。
- 线路延迟：指在设计之中，时钟消耗的延迟。
线路延迟在设计内部，可通过PR工具依据走线和线路上的元器件，计算出线路延迟。源延迟是设计外部的时钟延迟，需要人为估计并告知工具。 ^clock-latency
